# 权限配置按本地、项目、用户三级加载；冲突时 deny 优先于 allow。
# 只读工具默认允许，allow 通常用于提前放行 Bash/Write；deny 可围栏敏感读取。
default_mode: default
permissions:
  allow:
    - Bash(git *)
    - Bash(pytest)
  ask:
    - Bash(git push *)
  deny:
    - Bash(rm *)
    - Read(.env)
    - Write(.env)
