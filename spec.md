# ArkCode 交互式 AI 对话基础 Spec

## 背景

ArkCode 的长期方向是成为一款运行在终端中的 AI 编程助手。当前项目只有基础命令行入口，尚不具备交互式界面、模型调用、流式输出或会话管理能力。

本阶段先建立后续 Agent 能力所依赖的对话基础：用户启动 ArkCode 后进入全屏终端界面，与大模型进行连续对话。系统支持 Anthropic Claude 和 OpenAI 两种后端，但暂不执行工具调用、文件操作或代码编辑。

## 目标

- 提供专注单栏的全屏终端对话界面，使用户可以输入消息并阅读模型回复。
- 模型生成内容到达后立即显示，实现真正的流式交互，而不是等待完整回复。
- 在当前运行期间维护多轮对话上下文，使模型能够理解此前消息。
- 支持通过单个 `.env` 配置选择 Anthropic 或 OpenAI 后端。
- 支持 Claude adaptive thinking，并将思考内容与最终回复分开展示。
- 建立与具体供应商协议解耦的 Provider 边界，为以后增加新后端和 Agent 能力提供稳定基础。
- 为启动、会话、Provider 和流式处理等核心流程提供可持久化的详细日志，使开发者可以检查原始输入输出、事件转换、执行结果和耗时。

## 功能需求

- F1：ArkCode 启动时从单个 `.env` 文件读取当前配置。配置包含 `name`、`protocol`、`model`、`base_url`、`api_key`，以及可选的 `thinking`。缺少必填项、值格式错误或协议不受支持时，应在发起模型请求前给出明确错误。

- F2：成功加载配置后，ArkCode 进入专注单栏的全屏终端界面。界面顶部显示当前配置名称、协议和模型，中间显示可滚动的对话记录，底部提供固定输入区和当前状态。

- F3：用户可以在输入区提交问题。首版提供 `/clear` 和 `/quit` 两个内置命令：`/clear` 清空当前运行中的对话记录及模型上下文；`/quit` 正常退出 ArkCode。

- F4：当 `protocol` 为 `anthropic` 时，ArkCode 使用 Anthropic Messages 协议；当 `protocol` 为 `openai` 时，使用 OpenAI Responses 协议。两种协议向上提供一致的对话行为，TUI 不因后端不同而改变操作方式。

- F5：两种后端均通过 SSE 接收生成事件。收到文本增量后，ArkCode 应立即更新当前回复，不等待完整响应结束；流结束后，将完整回复加入当前会话上下文。

- F6：ArkCode 在当前进程内维护多轮对话历史。每次正常完成的请求都包含此前有效轮次，使模型能结合之前的对话回答。退出程序后不保存可恢复的会话状态，重新启动时进入新会话；持久化运行日志不得被用来恢复模型上下文。

- F7：当 Anthropic 配置中的 `thinking=true` 时，ArkCode 请求 Claude 使用 adaptive thinking，并在最终回复之前以独立、弱化样式的行内 Thinking 区块流式展示思考内容。当 `thinking` 为 `false` 或省略时不启用该能力。配置的模型不支持 adaptive thinking 时，应明确报错，不得静默降级。OpenAI 配置不使用该字段。

- F8：模型正在生成时，用户按 `Ctrl+C` 可以取消当前请求。界面保留已经显示的用户消息、thinking 和部分回复，并将该轮标记为“已取消”；本轮用户消息和不完整回复不进入后续模型上下文，ArkCode 返回可继续输入的状态。

- F9：模型回复在生成过程中持续按 Markdown 渲染，包括标题、列表、链接和代码块；代码块应保持可读的终端样式。Thinking 区块与最终回答在视觉上明确区分。

- F10：空闲状态下按 `Ctrl+C` 或执行 `/quit` 时，ArkCode 正常退出并恢复终端。API 认证失败、限流、网络中断、服务端错误或流式协议错误应显示可理解的错误信息，并在可以安全恢复时允许用户继续对话。失败轮次不进入后续上下文。

- F11：ArkCode 每次启动时生成一个唯一 `session_id`，并在项目根目录的 `logs/` 下创建以 `<session_id>.log` 命名的持久化日志文件。执行 `/clear` 不更换 `session_id` 或日志文件。

- F12：日志采用 JSON Lines 格式，每条记录包含 `timestamp`、`level`、`session_id`、`component`、`event`、`status` 和 `message`；根据事件需要，可附加 `turn_id`、`provider`、`protocol`、`model`、`sequence`、`request_id`、`response_id`、`method`、`args`、`result`、`elapsed_ms`、`raw_event`、`normalized_event`、`content_type`、`delta`、`sse_event_count`、`error` 和 `redacted_fields`。

- F13：日志覆盖启动、配置加载、Provider 创建、TUI 就绪、用户输入、请求构造、每个原始 SSE 事件、统一事件转换、thinking/text 增量、最终聚合结果、上下文提交或回滚、清空、取消、错误和退出等核心节点。日志保留完整用户消息、thinking、模型回复以及非敏感的原始方法入参和出参；流结束、取消或失败时，还应记录截至当时聚合出的完整或部分结果、事件总数、停止原因、执行状态和耗时。

- F14：API Key、认证请求头、Bearer Token 和其他认证秘密在写入日志前必须替换为 `[REDACTED]`。发生脱敏的记录应通过 `redacted_fields` 指明被替换的字段路径。

## 非功能需求

- N1：TUI 在等待网络响应和接收流式内容期间必须保持可响应，用户应能滚动历史、观察状态并取消当前请求，不能因模型调用而冻结界面。

- N2：流式增量到达后应及时进入界面更新队列。为保证 Markdown 渲染稳定，可以合并过于密集的增量更新，但不得等待完整响应后才显示。

- N3：Provider 边界应屏蔽供应商请求格式、认证方式和流事件差异。增加新 Provider 时，不应要求修改会话的基本行为或重新设计 TUI。

- N4：`.env` 中的 API Key 不得显示在界面、正常日志或错误信息中，也不得提交进版本控制。配置错误信息只指出缺失或无效的字段。

- N5：程序应在受支持的 macOS 和 Linux 现代终端中正确启动、处理常用按键并在退出后恢复终端状态。

- N6：正常退出、用户取消、API 错误和意外异常均不得令终端停留在损坏的全屏、隐藏光标或错误输入模式。

- N7：双 Provider 的协议转换、会话管理、取消回滚和配置校验应可通过不依赖真实网络的自动化测试验证；最终验收还应分别使用 Anthropic 与 OpenAI 的真实 API 完成端到端对话。

- N8：真实 API 验收所用凭据只从用户本地环境读取。测试证据不得包含完整 API Key、认证请求头或其他秘密。

- N9：本阶段不自动压缩、总结或裁剪长会话。当上下文超过模型或供应商限制时，应显示明确错误并保持 TUI 可继续使用，不得悄悄丢弃历史消息。

- N10：运行时状态必须清晰区分空闲、请求中、thinking、流式回答、已取消和错误，避免用户误判 ArkCode 是否仍在工作。

- N11：日志文件和 `logs/` 目录不得提交进版本控制。日志虽然包含原始对话内容，但只用于开发观测，不构成正式的会话持久化格式或兼容性承诺。

- N12：启动时如果无法创建或写入本次 session 的日志文件，ArkCode 应拒绝启动并明确报告原因。运行过程中发生日志写入失败时，应在 TUI 和标准错误中告警，但不得立即中断正在进行的模型回复。

- N13：逐事件日志记录不得阻塞网络事件消费或令 TUI 明显失去响应；日志事件顺序必须足以还原同一轮请求的接收与转换顺序。

## 不做的事

- 不实现 tool use、函数调用、Shell 命令执行或任何 Agent 行动循环。
- 不读取、搜索、创建、修改或删除用户文件，也不进行代码编辑。
- 不保存或恢复历史会话，不提供多个会话的创建、命名、切换或管理。
- 不使用运行日志恢复会话或模型上下文，也不承诺日志格式可作为未来会话持久化格式。
- 不在一个 `.env` 中保存多个配置，也不在运行期间切换 Provider、模型或配置文件。
- 不提供 `/help`、`/model`、`/status` 等扩展命令；首版仅支持 `/clear` 和 `/quit`。
- 不将 `thinking` 配置映射为 OpenAI reasoning；该配置仅控制 Anthropic adaptive thinking。
- 不支持图片、音频、附件或其他多模态输入输出，仅支持文本对话。
- 不实现上下文自动总结、压缩、裁剪或 token 预算管理。
- 不自动续写被取消或因网络中断而停止的回复。
- 不提供 API Key 管理界面、系统钥匙串集成或远程配置中心。
- 不承诺 Windows 终端兼容性。
- 不提供用量统计、费用估算、遥测、账号管理或供应商管理界面。
- 不实现日志轮转、压缩、上传、自动清理或保留期限管理。

## 验收标准

- AC1：分别使用缺少必填字段、未知 `protocol` 和无效 `thinking` 值的 `.env` 启动 ArkCode，程序在发送任何 API 请求前指出具体配置问题，且错误中不包含 API Key。

- AC2：使用有效配置启动后进入全屏单栏 TUI；可以观察到顶部配置状态、中间可滚动对话区、底部输入区，以及当前运行状态。

- AC3：在已有多轮对话时执行 `/clear`，界面对话记录和后续请求上下文均被清空；执行 `/quit` 后程序正常退出并恢复终端。

- AC4：分别配置 `protocol=anthropic` 与 `protocol=openai`，能够通过相同的 TUI 操作完成真实对话；前者调用 Anthropic Messages，后者调用 OpenAI Responses。

- AC5：对两个真实 Provider 分别发起能够产生较长回答的请求，在响应尚未结束时可以观察到内容多次增量更新，证明回复通过 SSE 流式显示而非完成后一次性出现。

- AC6：在同一次运行中先告诉模型一个临时事实，再通过后续问题询问该事实，两个 Provider 均能结合此前对话回答；退出并重新启动后，该事实不再存在于新会话上下文中。

- AC7：使用支持 adaptive thinking 的 Claude 模型并配置 `thinking=true` 后，可以先观察到独立的 Thinking 流式区块，再看到最终回答；配置为 `false` 或省略时不出现该区块；模型不支持时显示明确错误。

- AC8：在模型流式生成期间按 `Ctrl+C`，请求停止，已显示内容保留并标记“已取消”，输入区重新可用；检查下一次请求可确认被取消轮次未进入有效上下文。

- AC9：让模型返回包含标题、列表、链接和带语言标记代码块的内容，生成期间这些内容持续按 Markdown 更新，完成后布局和代码块保持清晰可读。

- AC10：分别模拟认证失败、限流、网络中断、服务端错误和格式错误的流事件，TUI 显示可理解的错误，失败轮次不进入上下文，并允许用户继续输入或安全退出。

- AC11：在空闲、生成中、取消后和错误后分别退出 ArkCode，终端均恢复正常输入、光标和显示状态。

- AC12：运行自动化测试和两家真实 API 的端到端验收，测试输出、界面和错误信息中均不出现完整 API Key 或认证请求头。

- AC13：模拟上下文超出供应商限制，ArkCode 明确报告限制错误，不自动删除历史，并保持界面可继续清空会话或退出。

- AC14：启动 ArkCode 后可以在项目根目录的 `logs/` 中找到以当前 `session_id` 命名的 `.log` 文件；执行 `/clear` 前后的事件继续写入同一文件，退出后文件仍然存在。

- AC15：完成一次包含 Claude thinking 的真实流式对话后，日志中可以按 `session_id`、`turn_id` 和 `sequence` 查看每个原始 SSE 事件及对应的统一事件，并在流结束记录中看到完整聚合 thinking、完整回复、事件总数、停止原因、成功状态和耗时。

- AC16：检查启动、配置、Provider 创建、TUI、正常对话、取消回滚、清空、错误和退出流程的日志，每个核心节点均包含适用的组件、事件、状态、说明、入参、出参或错误信息。

- AC17：使用包含可识别测试秘密的配置运行成功与失败场景，日志中保留完整用户消息和模型输出，但 API Key、认证请求头和 Token 均显示为 `[REDACTED]`，且 `redacted_fields` 指出相应字段。

- AC18：令 `logs/` 在启动前不可写时，ArkCode 在进入 TUI 前明确失败；模拟运行中日志写入失败时，TUI 和标准错误出现告警，正在生成的回复不被立即终止。
